---
import LogoMark from "./LogoMark";
import VideoField from "./VideoField.astro";
import { brand, contact, nav } from "../data/content";

const year = new Date().getFullYear();
---

<footer class="relative overflow-hidden bg-ink py-14">
  <VideoField opacity={0.28} lift={0.2} veil presence={false} />
  <div class="relative z-10 mx-auto max-w-6xl px-5 sm:px-8">
    <div class="flex flex-col gap-10 border-b border-ink-border pb-10 sm:flex-row sm:justify-between">
      <div class="max-w-xs">
        <a href="#top" class="flex items-center gap-2.5">
          <LogoMark size={32} />
          <span class="font-display text-lg italic text-cream">{brand.name}</span>
        </a>
        <p class="mt-4 text-sm text-dusk-100/60">{brand.location}</p>
      </div>
      <div class="flex flex-wrap gap-x-12 gap-y-6">
        <div>
          <h4 class="text-xs uppercase tracking-wide text-dusk-100/50">Navigate</h4>
          <ul class="mt-4 flex flex-col gap-2.5">
            {
              nav.map((n) => (
                <li>
                  <a href={n.href} class="-my-2 inline-block py-3 text-sm text-dusk-100/80 transition-colors hover:text-cream">
                    {n.label}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>
        <div>
          <h4 class="text-xs uppercase tracking-wide text-dusk-100/50">Contact</h4>
          <ul class="mt-4 flex flex-col gap-2.5">
            <li>
              <a
                href={contact.bookingUrl}
                target="_blank"
                rel="noopener noreferrer"
                data-calendly
                data-ga-event="book_call"
                class="-my-2 inline-block py-3 text-sm text-dusk-100/80 transition-colors hover:text-cream"
              >
                Book a call
              </a>
            </li>
            <li>
              <a
                href={contact.whatsapp}
                target="_blank"
                rel="noopener noreferrer"
                data-ga-event="whatsapp_click"
                class="-my-2 inline-block py-3 text-sm text-dusk-100/80 transition-colors hover:text-cream"
              >
                WhatsApp
              </a>
            </li>
            <li>
              <a href={`mailto:${contact.email}`} data-ga-event="email_click" class="-my-2 inline-block py-3 text-sm text-dusk-100/80 transition-colors hover:text-cream">
                {contact.email}
              </a>
            </li>
          </ul>
        </div>
      </div>
    </div>
    <p class="mt-8 text-xs text-dusk-100/40">© {year} {brand.name}. All rights reserved.</p>
  </div>
</footer>
